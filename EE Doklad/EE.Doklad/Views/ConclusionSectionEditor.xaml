<UserControl x:Class="EE.Doklad.Views.ConclusionSectionEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EE.Doklad.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:ConclusionSectionViewModel}">
    <Grid Background="White">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Margin="20">
                <!-- Заглавие -->
                <TextBlock Text="{Binding Data.Title}" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Margin="0,0,0,20"/>

                <!-- Описание (ако има) -->
                <TextBlock Text="{Binding Data.Description}" 
                           FontSize="13"
                           Foreground="#666666"
                           TextWrapping="Wrap"
                           Margin="0,0,0,15"
                           Visibility="{Binding Data.Description, Converter={StaticResource StringToVisibilityConverter}}"/>

                <!-- Редактируем текст -->
                <TextBlock Text="Заключителен текст:" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,10"/>

                <TextBox Text="{Binding Data.ConclusionText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="400"
                         MaxHeight="600"
                         Padding="15"
                         FontSize="13"
                         Background="#FAFAFA"
                         BorderBrush="#CCCCCC"
                         BorderThickness="1"
                         VerticalAlignment="Stretch"/>

                <!-- Информация за потребителя -->
                <TextBlock Text="💡 Текстът по-горе може да се редактира според нуждите на проекта."
                           FontSize="11"
                           Foreground="#888888"
                           FontStyle="Italic"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
