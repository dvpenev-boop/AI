<Window x:Class="EE.Doklad.Views.MaterialsEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EE.Doklad.ViewModels"
        mc:Ignorable="d"
        Title="Редакция материал" Height="450" Width="700">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Наименование:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="400" Text="{Binding Material.NameBg, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <GroupBox Header="Варианти" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding Variants}"
                          SelectedItem="{Binding SelectedVariant, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          Margin="4">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ρ [kg/m³]" Binding="{Binding RhoKgM3, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                        <DataGridTextColumn Header="c [J/kgK]" Binding="{Binding CJKgK, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                        <DataGridTextColumn Header="λ [W/mK]" Binding="{Binding LambdaWMK, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                        <DataGridTextColumn Header="μ" Binding="{Binding Mu, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="4">
                    <Button Content="Добави вариант" Command="{Binding AddVariantCommand}" Margin="0,0,8,0"/>
                    <Button Content="Премахни вариант" Command="{Binding RemoveVariantCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,10,0,0">
            <Button Content="ОК" Width="90" Margin="0,0,8,0" Click="Ok_Click"/>
            <Button Content="Отказ" Width="90" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
