<UserControl x:Class="EE.Doklad.Views.RoofSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EE.Doklad.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/EE.Doklad;component/Views/RoofSectionViewResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.DataContext>
        <vm:RoofSectionViewModel />
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Покрив" FontSize="22" FontWeight="Bold" />
        <!-- Описание -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,10">
            <TextBlock Text="Описание на покрива (вид, състояние, конструкция):" />
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="60"/>
        </StackPanel>
        <!-- Обобщаваща таблица -->
        <GroupBox Header="Покрив по типове" Grid.Row="2">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,5">
                    <Button Content="Добави топъл покрив" Command="{Binding AddWarmRoofCommand}" Margin="0,0,5,0"/>
                    <Button Content="Добави студен покрив" Command="{Binding AddColdRoofCommand}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding RoofTypes}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding Number}" IsReadOnly="True" Width="40"/>
                        <DataGridTextColumn Header="Тип покрив" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        <DataGridTextColumn Header="Режим" Binding="{Binding Mode}" IsReadOnly="True" Width="80"/>
                        <DataGridTextColumn Header="U (W/m²K)" Binding="{Binding U}" IsReadOnly="True" Width="100"/>
                        <DataGridTextColumn Header="A (m²)" Binding="{Binding A, UpdateSourceTrigger=PropertyChanged, StringFormat=N3}" Width="100"/>
                        <DataGridTemplateColumn Header="" Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Премахни" Command="{Binding DataContext.RemoveRoofTypeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <!-- Детайли за всеки тип покрив -->
                <ItemsControl ItemsSource="{Binding RoofTypes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource RoofDetailTemplateSelector}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
