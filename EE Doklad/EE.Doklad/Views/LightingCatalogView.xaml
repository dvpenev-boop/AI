<UserControl x:Class="EE.Doklad.Views.LightingCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EE.Doklad.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.DataContext>
        <vm:LightingCatalogViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Осветителни компоненти" FontSize="18" FontWeight="Bold" />
                <TextBlock Text="Нормативните компоненти са само за преглед. Потребителските могат да се добавят/изтриват." FontSize="12" Foreground="DimGray"/>

                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Импортиране" x:Name="ImportButton" Margin="0,0,6,0" Padding="8,4" />
                    <Button Content="Експортиране" x:Name="ExportButton" Margin="0,0,6,0" Padding="8,4" />
                    <Button Content="+ Добави (потребителски)" Command="{Binding AddUserItemCommand}" Margin="0,0,6,0" Padding="8,4"/>
                    <Button Content="🗑 Изтрий" Command="{Binding DeleteSelectedCommand}" Padding="8,4"/>
                </StackPanel>
            </StackPanel>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                    <CheckBox Content="Нормативни" IsChecked="{Binding ShowSeed}" Margin="0,0,6,0"/>
                    <CheckBox Content="Потребителски" IsChecked="{Binding ShowUser}"/>
                </StackPanel>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" Padding="10">
            <DataGrid ItemsSource="{Binding Items}"
                      SelectedItem="{Binding Selected}"
                      AutoGenerateColumns="False"
                      IsReadOnly="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeRows="False"
                      RowEditEnding="DataGrid_RowEditEnding">
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="IsEnabled" Value="True" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSeed}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Seed" Binding="{Binding IsSeed}" Width="60" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Наименование" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="3*"/>
                    <DataGridTextColumn Header="Мощност [W]" Binding="{Binding PowerW, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
