<Window x:Class="EE.Doklad.Views.InsertSectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вмъкване на нова секция" 
        Height="280" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Номер на секция -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Номер на секция:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SectionNumberTextBox" 
                         Grid.Column="0"
                         TextChanged="SectionNumberTextBox_TextChanged"
                         PreviewTextInput="NumberValidationTextBox"/>
                <TextBlock Grid.Column="1" 
                           x:Name="RangeHintTextBlock"
                           VerticalAlignment="Center"
                           Margin="12,0,0,0"
                           Foreground="#666"
                           FontSize="12"/>
            </Grid>
        </StackPanel>

        <!-- Заглавие -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Заглавие на секция:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBox x:Name="SectionNameTextBox" 
                     TextChanged="SectionNameTextBox_TextChanged"/>
        </StackPanel>

        <!-- Преглед -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="Преглед:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Border Background="White" 
                    BorderBrush="#DDD" 
                    BorderThickness="1" 
                    CornerRadius="4"
                    Padding="12">
                <TextBlock x:Name="PreviewTextBlock" 
                           FontSize="16"
                           FontWeight="Medium"
                           Foreground="#333"/>
            </Border>
        </StackPanel>

        <!-- Бутони -->
        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Добави" 
                    x:Name="OkButton"
                    IsDefault="True"
                    Click="OK_Click"
                    Background="#0078D4"
                    Foreground="White"
                    Margin="0,0,10,0">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Отказ" 
                    IsCancel="True"
                    Click="Cancel_Click"
                    Background="#E1E1E1"
                    Foreground="#333">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D1D1D1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
