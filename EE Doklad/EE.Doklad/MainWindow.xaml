<Window x:Class="EE.Doklad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EE.Doklad"
        xmlns:vm="clr-namespace:EE.Doklad.ViewModels"
        mc:Ignorable="d"
        Title="EE Доклад - Енергиен доклад" Height="700" Width="1200">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Меню -->
        <Menu Grid.Row="0">
            <MenuItem Header="_Файл">
                <MenuItem Header="_Нов доклад" Command="{Binding NewReportCommand}"/>
                <MenuItem Header="_Отвори..." Command="{Binding OpenReportCommand}"/>
                <MenuItem Header="_Запази..." Command="{Binding SaveReportCommand}"/>
                <Separator/>
                <MenuItem Header="Из_ход" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_Експорт">
                <MenuItem Header="Експорт в _PDF..." Command="{Binding ExportToPdfCommand}"/>
            </MenuItem>
            <MenuItem Header="_Помощ">
                <MenuItem Header="_За програмата" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <!-- Основен layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Лява панел - секции -->
            <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="0,0,1,0" Padding="10">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="{Binding CurrentReport.Title}" 
                               FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <Button DockPanel.Dock="Top" Content="+ Добави секция" 
                            Command="{Binding AddSectionCommand}" Margin="0,0,0,10"/>
                    
                    <ListBox ItemsSource="{Binding CurrentReport.Sections}"
                             SelectedItem="{Binding SelectedSection}"
                             DisplayMemberPath="Title"/>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

            <!-- Дясна панел - съдържание на секцията -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20" DataContext="{Binding SelectedSection}">
                    <TextBlock Text="Избрана секция" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Заглавие:"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>
                    
                    <TextBlock Text="Статичен текст:"/>
                    <TextBox Text="{Binding StaticText, UpdateSourceTrigger=PropertyChanged}" 
                             Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                             VerticalScrollBarVisibility="Auto" Margin="0,5,0,15"/>
                    
                    <TextBlock Text="Таблици:" FontWeight="Bold" Margin="0,10,0,10"/>
                    
                    <ItemsControl ItemsSource="{Binding Tables}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                        <DataGrid ItemsSource="{Binding Rows}" 
                                                  AutoGenerateColumns="False" 
                                                  CanUserAddRows="False"
                                                  HeadersVisibility="Column">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="Кол. 1" Binding="{Binding Cells[0].Value}" Width="*"/>
                                                <DataGridTextColumn Header="Кол. 2" Binding="{Binding Cells[1].Value}" Width="*"/>
                                                <DataGridTextColumn Header="Кол. 3" Binding="{Binding Cells[2].Value}" Width="*"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
